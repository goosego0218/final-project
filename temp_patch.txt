*** Begin Patch
*** Update File: app/main.py
@@
-    if edit_image_url:
-        try:
-            source_image = requests.get(edit_image_url, timeout=60)
-            source_image.raise_for_status()
-        except requests.RequestException as exc:
-            raise HTTPException(
-                status_code=500, detail=f"Failed to download edit image: {exc}"
-            ) from exc
-
-        files = {
-            "image": ("edit.png", source_image.content, "image/png"),
-        }
+    if edit_image_url:
+        image_bytes: bytes
+        if edit_image_url.startswith("http"):
+            try:
+                source_image = requests.get(edit_image_url, timeout=60)
+                source_image.raise_for_status()
+                image_bytes = source_image.content
+            except requests.RequestException as exc:
+                raise HTTPException(
+                    status_code=500, detail=f"Failed to download edit image: {exc}"
+                ) from exc
+        else:
+            try:
+                image_path = Path(edit_image_url)
+                image_bytes = image_path.read_bytes()
+            except OSError as exc:
+                raise HTTPException(
+                    status_code=500, detail=f"Failed to read edit image: {exc}"
+                ) from exc
+
+        files = {
+            "image": ("edit.png", image_bytes, "image/png"),
+        }
*** End Patch
